{
  "name": "tagcache",
  "version": "0.1.0",
  "description": "Tiny in-memory cache for Node/TS — base implementation (pre-TTL, pre-tags).",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc -p .",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "5.4.5",
    "vitest": "1.6.0",
    "@types/node": "20.12.7"
  }
}
